import 'package:flutter/material.dart';

class Unknownscreen extends StatelessWidget {
  static String routeNeme = "/UnknownScreen";
  const Unknownscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
